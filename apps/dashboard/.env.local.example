# Copy to .env.local. Server-side only (no NEXT_PUBLIC_ prefix) so the raw
# API key never reaches the browser — app/api/proxy/[...path]/route.ts is
# the only thing that reads these.
API_BASE_URL=http://localhost:8000
API_KEY=rk_live_replace_with_a_real_demo_merchant_key
